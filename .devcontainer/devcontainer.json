{
    "name": "Stellar CLI Dev",
    "image": "mcr.microsoft.com/devcontainers/rust:1-1-bullseye",
    "postCreateCommand": "rustup target add wasm32-unknown-unknown && cargo install --locked stellar-cli --features opt && stellar --version"
}
